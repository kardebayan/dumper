name: Dump
 
on:
  push:
    paths-ignore:
      - '*.md'
    branches: [ main ]

defaults:
  run:
    shell: bash

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
       - name: Checkouts
         uses: actions/checkout@v2

       - name: Clone Dumpyara
         run: |
              git clone https://github.com/AndroidDumps/dumpyara.git

       - name: Setup Dumpyara environment
         working-directory: dumpyara
         run: |
              sudo bash setup.sh
              git config --global user.name "ksauraj1"
              git config --global user.email "kumarsauraj24@mymail.infos.st"

       - name: Setup config envs
         working-directory: dumpyara
         run: |
              echo "${{ secrets.GIT_TOKEN }}" > .github_token
              echo "${{ secrets.GIT_ORG_NAME }}" > .github_orgname
              echo "${{ secrets.BOT_TOKEN }}" > .tg_token
              echo "${{ secrets.CHAT_ID }}" > .tg_chat

       - name: Running Dumpyara
         working-directory: dumpyara
         run: |
              ./dumpyara.sh "$ROM_URL"
#
env:
  ROM_URL: http://otafsg1.realmemobile.com/patch/amazone2/GLO/RMX1831EX/RMX1831EX_11.C.20_0200_202007222131/RMX1831EX_11_OTA_0200_all_QrdLEyEpDpi6_8b0ef6b02d6f675.ozip
